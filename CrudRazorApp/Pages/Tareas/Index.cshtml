@page
@model CrudRazorApp.Pages.Tareas.IndexModel
@{
    ViewData["Title"] = "Gestor de Proyectos";
}

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="~/css/IndexTareas.css" />

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="mb-0">Gestor de Tareas</h1>
    <a class="btn btn-success" asp-page="Create">➕ Nueva Tarea</a>
</div>

<!-- Filtros -->
<div class="filter-box">
    <button class="btn">Categoría</button>
    <button class="btn">Integrante</button>
    <button class="btn">Estado</button>
</div>

<!-- Tabla -->
<div class="custom-table-container">
    <table class="custom-table">
        <thead>
            <tr>
                <th>ID Proyecto</th>
                <th>Tarea</th>
                <th>Fecha inicio</th>
                <th>Fecha finalización</th>
                <th>Integrante</th>
                <th>Estatus</th>
                <th>Descripción</th>
                <th class="text-end">Acciones</th>
            </tr>
        </thead>
        @*
        <tbody>
            @foreach (var item in Model.ListaProductos)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Nombre</td>
                    <td>@item.Precio</td>
                    <td>@item.Precio</td>
                    <td>@item.Precio</td>
                    <td>@item.Precio</td>
                    <td>@item.Precio</td>
                    <td class="text-end">
                        <a asp-page="Details" asp-route-id="@item.Id" class="btn btn-info btn-sm">Detalles</a>
                        <a asp-page="Edit" asp-route-id="@item.Id" class="btn btn-warning btn-sm">Editar</a>
                        <a asp-page="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm">Eliminar</a>
                    </td>
                </tr>
            }
        </tbody>
        *@

    </table>
</div>